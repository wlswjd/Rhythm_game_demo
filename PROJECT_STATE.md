# PROJECT_STATE

이 문서는 프로젝트의 현재 상태 기준이다. 결정이 바뀌면 이 문서를 갱신한다.

최종 갱신: 2026-09-08

---

## 1. 나에 대한 정보

- 게임 기획·개발로 진로를 잡은 취준생. 게임 개발은 처음.
- 목표 직군: 게임 기획(1순위). 목표 회사에 펄어비스, 크래프톤, 컴투스 포함.
- 배경: 기업 부설연구소 R&D 약 2.5년(IoT 센서 데이터 검증, TTA 인증, 정부 과제 커뮤니케이션),
  AI 부트캠프 수료(추천시스템 대회 1위, CV·LLM 요약·부동산 예측),
  학점은행제로 CS 학위 진행 중.
- 차별점으로 삼으려는 것: 밸런스·난이도 설계에 데이터 근거를 넣는 것.
- 작업 가능 시간: 하루 약 4시간(순수 구현 3시간 기준으로 일정을 잡는다).
- Unity를 예전에 잠깐 다뤄본 경험이 있음.

---

## 2. 만들려는 게임

**월드를 돌아다니며 스토리를 진행하고, NPC 등과 상호작용하다가
보스전/중간보스전을 리듬게임으로 진행하는 2D 어드벤처.**

- 리듬 파트는 전체 게임의 전투 서브시스템에 해당한다. 순수 리듬게임이 아니다.
- 리듬 코어는 "박 세기형"(리듬닥터식 원버튼). 노트 낙하형이 아니다.
  스페이스바 하나로 7번째 박에 맞춰 누르는 방식.
- 세계관은 한국 토속신화 기반 오컬트 방향을 구상 중이나 아직 확정 전.
  NPC 대사를 채우는 단계에서 확정하면 된다.

---

## 3. 확정된 결정

| 항목 | 결정 | 비고 |
|---|---|---|
| 엔진 | **Unity (C#)** | 2026-07-28 확정. 재검토 금지 |
| Unity 버전 | **Unity 6.3 LTS (6000.3.23f1)** | 2027년 12월까지 지원. 6.0 LTS는 2026년 10월 종료라 배제 |
| 템플릿 | **Universal 2D (URP)** | 2D Light와 Pixel Perfect Camera 때문. Built-in 아님 |
| 프로젝트명 | **Rhythm_game_demo** | 로컬·GitHub 동일 |
| 코드 작성 | **Cursor 등 AI 활용** | 아래 단서 참조 |
| 시점 | **직교 격자 로직 + 3/4 시점 아트** | 진짜 아이소메트릭 아님. 컵헤드 월드맵과 같은 방식 |
| 타일 크기 | 32px | 에셋 조달 시 이 규격에 맞출 것 |
| 리듬 코어 | 박 세기형(원버튼, 7박) | |
| 난이도 축 | 리듬 구조(정박 → 변박 → 무음 박 → 폴리리듬) | 음악 장르가 아니라 리듬 구조로 난이도를 나눈다 |
| 음악 장르 | 연출 레이어로만 사용 | 난이도 기준이 아님 |
| 테스트 곡 | Electrodoodle / Kevin MacLeod / incompetech | 120 BPM, 4/4, 2분 46초, CC BY(저작자 표시 의무) |
| 곡 취급 | 소모품. 나중에 교체 전제 | BPM·오프셋을 설정으로 분리해 교체 비용을 0에 가깝게 유지 |
| 에셋 원칙 | 움직이는 것은 무료 에셋, 정지한 것은 AI | 아래 4절 참조 |

> **코드 AI 활용 원칙**
> 이동·충돌·카메라·UI 등 배관 코드는 AI에 위임한다.
> 오디오 클럭, 오프셋, 판정 윈도우, 난이도 파라미터의 **숫자는 직접 결정하고**
> 근거를 DEVLOG에 남긴다. 이 부분이 기획서의 원재료다.
> 한 번에 수백 줄을 받지 않는다. 스크립트 한 개 단위로 요청하고 읽고 넘어간다.

---

## 4. 에셋 방침

| 종류 | 조달 방법 | 이유 |
|---|---|---|
| 타일셋 | 무료 에셋 | 격자 정합·이음매 때문에 AI 생성 불가 |
| 캐릭터 스프라이트 | 무료 에셋 | 다방향·다프레임 일관성 때문에 AI 생성 불가 |
| 초상화·대화 얼굴 | AI 생성 | 단일 이미지라 가능 |
| 타이틀 아트·컷씬 | AI 생성 | 가능 |
| 정지 소품(장승, 솟대, 사당 등) | Meshy 등 3D 생성 후 단일 각도 렌더링 검토 | 무료 에셋에 없고 애니메이션 불필요 |
| 효과음 | 무료 라이브러리 | |

원칙:
- 한 제작자의 한 에셋 팩 안에서 최대한 해결한다. 여러 곳에서 모으면 톤이 깨진다.
- 한국 토속신화 분위기 타일셋은 사실상 없다. 일본풍 팩에 색보정을 걸고
  핵심 소품만 직접 추가하는 방식으로 접근한다.
- 한글 지원 픽셀 폰트를 미리 확보해 둘 것. 대부분의 픽셀 폰트는 한글을 지원하지 않는다.
- 받는 즉시 DEVLOG.md의 에셋 출처 표에 출처·라이선스·표기 의무를 기록한다.

---

## 5. 1차 목표: 월드 탐색 데모

리듬 보스전은 제외하고, 맵을 돌아다니는 부분을 먼저 완성품으로 만든다.

### 완료 정의 (이 6개가 끝나면 완성. 더 하지 않는다)

1. 맵 1개. 40x30 타일, 바닥/벽 두 종류만
2. 이동·충돌·카메라 추적
3. NPC 3명. 각자 대사 3~5줄
4. 대사창(여러 줄 넘기기, 대사 중 이동 불가)
5. 보스전 진입 트리거 — 실제 전투 없이 "보스전 시작" 문구만 표시
6. 타이틀 화면(시작 버튼 하나)

5번이 중요하다. 실제 전투는 안 만들지만 리듬 파트가 들어갈 연결부를 지금 만들어 둔다.

### 작업 블록

| 블록 | 내용 | 익히는 Unity 기능 |
|---|---|---|
| A | 스프라이트 하나를 방향키로 이동 | MonoBehaviour, Update, Inspector 노출, Play 모드 |
| B | 이동·충돌 | Rigidbody2D, Collider2D, Input System, FixedUpdate |
| C | 카메라 추적 | Cinemachine, Pixel Perfect Camera |
| D | 타일맵(플레이스홀더) | Tilemap, Tile Palette, Tilemap Collider 2D, Composite Collider |
| E | 아트 디렉션 + 에셋 조달 | 에셋스토어, 라이선스 표기 |
| F | 에셋 통합 | Sprite Editor 슬라이싱, Animator, Sorting Layer / Y축 깊이 정렬 |
| G | 대사 시스템 | Canvas, TextMeshPro, ScriptableObject, 상태 잠금 |
| H | NPC 3명 + 트리거 | Trigger Collider, 프리팹 |
| I | 타이틀·씬 전환·빌드 | SceneManager, Build Profiles |

D를 E보다 먼저 두는 이유: 에셋이 없어도 기본 사각형으로 맵을 깔 수 있다.
에셋을 기다리며 코드를 멈추지 않는다.

### 일정 견적

원래 견적 약 10일. **초보의 첫 프로젝트는 견적의 1.5~2배가 나온다.
실제로는 14~20일(3주)로 잡는다.**
리듬 보스전은 이후 별도로 1~1.5주. 포트폴리오 제출 가능 상태까지 총 5~7주 예상.

---

## 6. 현재 상태

- Unity 6.3 LTS(6000.3.23f1) 설치 완료. 모듈: Mac Build Support(IL2CPP),
  Windows Build Support(Mono). 한국어 언어팩 미설치(영어 UI 유지).
- Universal 2D(URP) 템플릿으로 프로젝트 생성 완료.
  경로: /Users/relaxman/Documents/Project/Rhythm_game_demo
- Git 연결 완료. .gitignore 적용 후 초기 커밋 및 push 완료.
  원격: https://github.com/wlswjd/Rhythm_game_demo (기본 브랜치 main)
- Unity AI 관련 패키지 제거. 미사용인데 NoSubscription 에러를 반복 출력해
  Console을 오염시켰다.
- 편집기 환경 구축 완료.
  - 편집기: Cursor
  - 연동: 비공식 패키지 `com.unity.ide.cursor` (git URL 설치)
  - .NET SDK 10.0.400 설치
  - Cursor C# 확장(Anysphere) 설치
  - `Reuse existing Cursor window` 체크. 자동완성 동작 확인 완료
- 아직 게임 코드는 한 줄도 없음. SampleScene에 Main Camera와 Global Light 2D만 존재.

### 다음 작업

**블록 A — 스프라이트 하나를 방향키로 움직이기 (60분)**
1. 흰 사각형 스프라이트를 씬에 배치
2. 방향키 입력을 받아 위치를 바꾸는 스크립트 작성
3. Play 버튼으로 동작 확인

---

## 7. 이미 검토하고 닫은 논의 (다시 열지 말 것)

이 항목들은 충분히 검토된 뒤 결정됐다. 내가 다시 꺼내면 이 문서를 근거로 제지할 것.

| 논의 | 결론 | 닫은 이유 |
|---|---|---|
| 엔진(웹 vs Unity) | Unity | 3회 재검토했다. 콘텐츠 저작 도구 이점 + 기존 Unity 경험 + 취업 신호 |
| 노트 낙하형 vs 박 세기형 | 박 세기형 | 스코프가 작고, 시선이 자유로워 스토리와 병존 가능 |
| 쯔꾸르(RPG Maker)로 전환 | 하지 않음 | 리듬 보스전 구현이 사실상 불가능. 커스텀 타일셋 부담이 오히려 큼 |
| 리듬게임을 기획서만 쓰고 끝내기 | 불가 | 게임이 돌아야 플레이 로그가 나오고, 로그가 없으면 데이터 근거 기획서를 쓸 수 없음 |
| 음악을 직접 제작해야 하는가 | 아니오 | 리듬게임의 핵심 기획 작업은 작곡이 아니라 채보 설계다 |
| 진짜 아이소메트릭 | 하지 않음 | 무료 에셋 희소성이 결정적. 코드는 Unity의 Isometric Tilemap으로 되지만, 그 각도로 그려진 무료 소품 팩이 사실상 없다. 블록 D 완료 직후 1회만 재검토 |
| AI 한 번에 게임 생성(GPT 원샷 데모) | 해당 없음 | 그 데모들은 단일 HTML/JS 산출물이라 Unity 프로젝트가 나오지 않는다. 그 경로는 이미 닫힌 웹 경로다. 또한 이 프로젝트의 난점은 코드량이 아니라 오디오 클럭·판정 윈도우 같은 숫자 판단이고, 기획 포트폴리오는 "돌아간다"가 아니라 "왜 이 숫자인가"로 평가된다 |

### 관찰된 패턴

낯선 작업(곡 선정, 오프셋 측정 등 판단이 필요한 작업) 앞에서 방향 전환 충동이
반복적으로 발생했다. 실제로 막힌 것이 아니라 마찰에 대한 반응이다.
이 신호가 나오면 하던 작업을 끝내는 쪽으로 유도할 것.

- 2026-09-08: 첫 구현 직전, GPT 원샷 게임 생성과 아이소메트릭 아트로
  두 번 화제가 이동했다. 둘 다 판단이 필요한 작업 앞에서 나왔다.

---

## 8. 작업 관리 규칙

- 하루 4시간: 20분 계획 → 90분 구현 → 휴식 → 85분 구현 → 30분 DEVLOG + 커밋
- 하루 3시간 중 코드 2시간 / 에셋·콘텐츠 1시간. 에셋 1시간 상한 엄수.
- 선택에 막히면 타임박스를 건다. 시간이 다 되면 그 시점의 후보로 확정하고 넘어간다.
- DEVLOG.md의 "결정과 근거" 항목이 기획서의 원재료다. 숫자를 정했으면
  왜 그 숫자인지 반드시 남긴다.
- 새 아이디어는 9절에 던져만 놓고 현재 작업을 끝낸다.
- 3절이나 6절이 실제로 바뀐 날에만 이 문서를 갱신하고 프로젝트 지식에 재업로드한다.
  코드만 만진 날은 DEVLOG.md만 쓴다.
- 커밋 메시지는 영어 또는 한글로 통일한다. 로마자 표기는 쓰지 않는다.
  이 레포는 채용 담당자가 열어 볼 대상이고 커밋 이력 자체가 자료가 된다.

---

## 9. 보류 중인 아이디어

현재 작업을 끝내기 위해 여기에만 적어 둔 것들. 지정된 시점에만 연다.

### 아트 레퍼런스 (블록 D 완료 직후 1회 검토)

- 아이소메트릭 픽셀아트 마을 이미지 2장을 참고로 봤다.
  마음에 든 지점: (조명 / 밀도 / 시점 중 무엇인지 직접 적을 것)
- 지적받은 문제: 두 이미지 모두 AI 생성 단일 일러스트라 타일셋으로 자를 수 없다.
  건물 15채 이상에 전부 다른 디자인이라 무료 에셋으로 재현 불가능한 밀도다.
  32px 타일 규격보다 훨씬 촘촘하다. 세계관도 서구 록 도시 / 유럽풍 판타지라
  한국 토속신화 오컬트와 접점이 없다.
- 되돌리기 비용: 블록 D까지는 낮다(플레이스홀더 맵 재작업 1~2시간).
  블록 E 이후에는 확보한 에셋 전체가 무용지물이 되어 급격히 커진다.
  따라서 블록 D 직후가 이 결정의 마감이다.

### Claude Code 도입 (블록 B 완료 후 검토)

- 로컬 파일을 직접 읽고 쓰므로 PROJECT_STATE.md, DEVLOG.md, C# 스크립트를
  복사-붙여넣기 없이 갱신할 수 있다.
- 지금 도입하지 않는 이유: 도구를 하나 더 붙이면 또 반나절이 간다.
  Unity 작업 흐름이 몸에 익은 뒤에 검토한다.