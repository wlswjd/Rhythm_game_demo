# PROJECT_STATE

이 문서는 프로젝트의 현재 상태 기준이다. 결정이 바뀌면 이 문서를 갱신한다.

최종 갱신: 2026-09-14

---

## 1. 나에 대한 정보

- 게임 기획·개발로 진로를 잡은 취준생. 게임 개발은 처음.
- 목표 직군: 게임 기획(1순위). 목표 회사에 펄어비스, 크래프톤, 컴투스 포함.
- 배경: 기업 부설연구소 R&D 약 2.5년(IoT 센서 데이터 검증, TTA 인증, 정부 과제 커뮤니케이션),
  AI 부트캠프 수료(추천시스템 대회 1위, CV·LLM 요약·부동산 예측),
  학점은행제로 CS 학위 진행 중.
- 차별점으로 삼으려는 것: 밸런스·난이도 설계에 데이터 근거를 넣는 것.
- 작업 가능 시간: 하루 약 4시간(순수 구현 3시간 기준으로 일정을 잡는다).
- Unity를 예전에 잠깐 다뤄본 경험이 있음.

---

## 2. 만들려는 게임

**제목(가제): Sound of Music**

**월드를 돌아다니며 스토리를 진행하고, NPC 등과 상호작용하다가
보스전/중간보스전을 리듬게임으로 진행하는 2D 어드벤처.**

- 리듬 파트는 전체 게임의 전투 서브시스템에 해당한다. 순수 리듬게임이 아니다.
- 리듬 코어는 "박 세기형"(원버튼). 노트 낙하형이 아니다.
  **단, 구체적 규칙은 미확정. 11절 참조.**
- 세계관 방향: **음악 장르별 월드 구상**으로 전환 검토 중.
  기존 "한국 토속신화 오컬트"는 보류. 11절에서 결론 낼 것.
  구매한 타일셋이 중세 유럽 판타지이므로 이쪽 정합성도 함께 고려한다.

---

## 3. 확정된 결정

### 3-1. 프로젝트 기반

| 항목 | 결정 | 비고 |
|---|---|---|
| 엔진 | **Unity (C#)** | 2026-07-28 확정. 재검토 금지 |
| Unity 버전 | **Unity 6.3 LTS (6000.3.23f1)** | 2027년 12월까지 지원 |
| 템플릿 | **Universal 2D (URP)** | 2D Light와 Pixel Perfect Camera 때문 |
| 프로젝트명 | **Rhythm_game_demo** | 로컬·GitHub 동일 |
| 코드 작성 | **Cursor 등 AI 활용** | 아래 단서 참조 |
| 시점 | **직교 격자 로직 + 3/4 탑다운 아트** | 아이소메트릭 아님. 2026-09-09 영구 종료 |

> **코드 AI 활용 원칙**
> 이동·충돌·카메라·UI 등 배관 코드는 AI에 위임한다.
> 오디오 클럭, 오프셋, 판정 윈도우, 난이도 파라미터의 **숫자는 직접 결정하고**
> 근거를 DEVLOG에 남긴다. 이 부분이 기획서의 원재료다.

### 3-2. 타일·좌표 규격

| 항목 | 결정 | 근거 |
|---|---|---|
| 타일 크기 | **16px** | 2026-09-09 32px에서 변경. 무료·저가 에셋 대부분이 16px. 32px 고수 시 선택지 급감 |
| 좌표 규격 | **1타일 = 1월드유닛** | 스프라이트 PPU를 타일 픽셀 크기와 일치시켜 유지 |
| 임포트 설정 | PPU 16 / Filter Mode Point / Compression None / Generate Physics Shape 해제 | 픽셀아트 고정 설정 |
| 카메라 | Orthographic Size 6 = 세로 12타일 | |

**Order in Layer 배치 규칙**

| 대상 | 값 |
|---|---|
| Tilemap_Ground | 0 |
| Tilemap_Collision | 1 |
| Player, NPC | 10 |
| UI, 이펙트 | 20 이상 |

### 3-3. 입력 설계

| 항목 | 결정 | 근거 |
|---|---|---|
| 입력 시스템 | **새 Input System** (Keyboard.current) | Unity 6 2D 템플릿 기본값 |
| 이동 | 방향키 | |
| 상호작용 | **E** | 스페이스는 리듬 판정 입력용으로 비워 둔다 |
| 대사 넘기기 | 스페이스 | 리듬 파트와 씬이 분리돼 충돌 없음 |

### 3-4. 리듬 파트

| 항목 | 결정 | 비고 |
|---|---|---|
| 난이도 축 | 리듬 구조(정박 → 변박 → 무음 박 → 폴리리듬) | 음악 장르가 아니라 리듬 구조로 난이도를 나눈다 |
| 음악 장르 | **역할 재검토 중** | 기존: 연출 레이어. 변경안: 월드·보스 구분 축. 11절 참조 |
| 테스트 곡 | Electrodoodle / Kevin MacLeod / incompetech | 120 BPM, 4/4, CC BY(저작자 표시 의무) |
| 곡 취급 | 소모품. 나중에 교체 전제 | BPM·오프셋을 설정으로 분리 |
| 시간 소스 | **AudioSettings.dspTime** | Time.time은 프레임 드랍에 밀리고, audioSource.time은 플랫폼별 갱신 주기가 불안정 |
| 오디오 임포트 | **Load Type = Decompress On Load** | Compressed In Memory는 재생 중 디코딩으로 타이밍이 흔들림 |

### 3-5. 에셋 방침

| 종류 | 조달 방법 | 이유 |
|---|---|---|
| 타일셋 | 무료·저가 에셋 | 격자 정합·이음매 때문에 AI 생성 불가 |
| 캐릭터 스프라이트 | 무료 에셋 | 다방향·다프레임 일관성 때문에 AI 생성 불가 |
| 초상화·대화 얼굴 | AI 생성 | 단일 이미지라 가능 |
| 타이틀 아트·컷씬 | AI 생성 | 가능. UI Image로 넣으면 2D Light 불필요 |
| 효과음 | 무료 라이브러리 | |

원칙:
- 한 제작자의 한 에셋 팩 안에서 최대한 해결한다. 여러 곳에서 모으면 톤이 깨진다.
- 한글 지원 픽셀 폰트를 미리 확보해 둘 것. 대부분의 픽셀 폰트는 한글을 지원하지 않는다.
- 받는 즉시 DEVLOG.md의 에셋 출처 표에 출처·라이선스·표기 의무를 기록한다.

**구매 완료**

| 에셋 | 제작자 | 라이선스 | 금액 |
|---|---|---|---|
| The Fan-tasy Tileset (Premium) 1.5.8, 16x16 | Ventilatore | 상업·비상업 사용 가능, 재판매·재배포 금지, 표기 의무 없음(권장) | $8 |

에셋 폴더 구조:
- `Tileset_` — 지형 기초(바닥, 잔디, 울타리). 맵의 토대
- `Atlas_` — 소품·나무·바위. 16x16 격자 고정
- `Objects` — 개별 오브젝트. 격자 무시 자유 배치, 콜라이더 포함
- `Tiled` 폴더는 Tiled 에디터 전용. Unity에서 쓰지 않는다

---

## 4. 1차 목표: 탐색 데모 + 리듬 보스전 1곡

**마감: 2026-09-30 (남은 기간 16일)**

### 완료 정의 (9개. 이게 끝나면 완성. 더 하지 않는다)

| # | 항목 | 상태 |
|---|---|---|
| 1 | 맵 2개. 각 40x30 타일. 두 맵 사이 이동 가능 | **미완** |
| 2 | 이동·충돌·카메라 추적 | 완료 |
| 3 | NPC 3명. 각자 대사 3~5줄 | 완료 |
| 4 | 대사창(여러 줄 넘기기, 대사 중 이동 불가) | 완료 |
| 5 | 보스전 진입 트리거 | 완료 |
| 6 | 타이틀 화면(시작 버튼 하나) | 완료 |
| 7 | 리듬 보스전 1개. 곡 1개, 60초, 난이도 1단계 | **미완 (착수)** |
| 8 | 판정 결과 로그 출력(판정별 횟수, 오차 ms) | **미완** |
| 9 | 기획서 1부. 게임 소개 + 리듬 코어 설명 + 판정 윈도우 근거 | **미완** |

**현재 5 / 9 완료.**

8번이 9번의 재료다. 로그가 없으면 데이터 근거 기획서를 쓸 수 없다.

### 남은 작업의 실제 무게

완료된 5개는 Unity에서 가장 정형화된 구간이었다.
남은 4개가 이 프로젝트의 본체이며, 특히 7·8·9번은 **숫자 판단**이 핵심이다.
1번(맵 확장)은 단순 작업량이지만 에셋 통합이 함께 걸려 있어 시간이 든다.

---

## 5. 현재 상태 (2026-09-14 기준)

### 구현 완료

- **씬 3개 분리**: Scene_Title(0) / Scene_World(1) / Scene_Boss(2). Build Profiles 등록 완료
- **이동·충돌**: Rigidbody2D + linearVelocity, FixedUpdate, Collision Detection Continuous
- **카메라**: Cinemachine 3. Lens Orthographic Size 6, Dead Zone 0.2, Damping 0.5
- **타일맵**: Tilemap_Ground / Tilemap_Collision 2층, Composite Collider(Merge)
- **타일셋 임포트**: Tileset_Ground.png 슬라이싱 완료. 플레이스홀더 교체 검증 완료
- **대사 시스템**: DialogueData(ScriptableObject) + DialogueManager(싱글턴) + 대사창 UI.
  대사 중 이동 잠금 동작 확인
- **NPC 3명**: 프리팹화 완료. Trigger Collider + E키 상호작용
- **보스 트리거**: 진입 시 대사 재생 + Debug.Log. 씬 전환은 미연결
- **타이틀**: 배경 + 제목 + START 버튼 → Scene_World 전환 동작 확인

### 미해결

- 맵이 1개, 크기 20x14 수준. 정식 크기(40x30) 미작업
- 지형 경계 타일 미적용. 현재 단색 잔디 + 단색 흙 테두리
- 소품·건물·캐릭터 스프라이트 미적용
- **한글 폰트 미확보.** TMP 기본 폰트가 한글 글리프를 갖고 있지 않아 네모로 출력됨
- 보스 트리거 → Scene_Boss 전환 미연결
- 카메라가 맵 밖(배경색)을 비춤

### 다음 작업

**리듬 코어 규칙 확정 (11절) → 블록 J**

11절이 닫히기 전까지 리듬 구현은 시작하지 않는다.
단, J-1(Conductor 스크립트, 오디오 클럭)은 규칙과 무관하므로 선행 가능.

---

## 6. 파일·폴더 구조

```
Assets/
├─ Audio/              곡 파일
├─ Dialogues/          DialogueData 애셋
├─ Palettes/
│   └─ Tiles/          타일 애셋
├─ Prefabs/            NPC 프리팹
├─ Scenes/             Scene_Title / Scene_World / Scene_Boss
├─ Scripts/
│   ├─ PlayerMovement.cs
│   ├─ DialogueData.cs
│   ├─ DialogueManager.cs
│   ├─ NPCInteraction.cs
│   ├─ BossTrigger.cs
│   ├─ SceneLoader.cs
│   └─ Conductor.cs
└─ Sprites/
    └─ Tileset/        임포트한 타일 시트
```

**명명 규칙 주의**
스프라이트 파일명과 타일 애셋명이 같으면 참조가 깨진다.
스프라이트는 `Sprite_` 접두사, 타일 애셋은 별도 폴더에 둔다.
(2026-09-09에 이 문제로 40분 소요)

---

## 7. 이미 검토하고 닫은 논의 (다시 열지 말 것)

| 논의 | 결론 | 닫은 이유 |
|---|---|---|
| 엔진(웹 vs Unity) | Unity | 3회 재검토했다 |
| 노트 낙하형 vs 박 세기형 | 박 세기형 | 스코프가 작고, 시선이 자유로워 스토리와 병존 가능 |
| 쯔꾸르(RPG Maker)로 전환 | 하지 않음 | 리듬 보스전 구현이 사실상 불가능 |
| 리듬게임을 기획서만 쓰고 끝내기 | 불가 | 게임이 돌아야 플레이 로그가 나온다 |
| 음악을 직접 제작해야 하는가 | 아니오 | 핵심 기획 작업은 작곡이 아니라 채보 설계다 |
| AI 한 번에 게임 생성 | 해당 없음 | 단일 HTML/JS 산출물. Unity 프로젝트가 나오지 않는다 |
| 진짜 아이소메트릭 | **영구 종료 (2026-09-09)** | 원하던 것은 시점이 아니라 입체감·상호작용·조망이며 셋 다 3/4 탑다운에서 해결됨. 컵헤드 월드맵도 아이소메트릭이 아님. 무료 소품 팩 부재가 결정적 |
| Rule Tile 오토타일 | 지금은 하지 않음 | 47개 규칙 등록에 2~3시간. 맵 2개 규모에서는 수동 배치가 빠름. 완료 정의에 없음 |
| 대사 선택지 분기 | 지금은 하지 않음 | 선택지 UI·커서·분기 처리 별도 필요, 견적 1일. 완료 정의에 없음 |

### 관찰된 패턴

낯선 작업(곡 선정, 오프셋 측정 등 판단이 필요한 작업) 앞에서 방향 전환 충동이
반복적으로 발생했다. 실제로 막힌 것이 아니라 마찰에 대한 반응이다.
이 신호가 나오면 하던 작업을 끝내는 쪽으로 유도할 것.

- 2026-09-08: 첫 구현 직전, GPT 원샷 게임 생성과 아이소메트릭 아트로 두 번 화제 이동
- 2026-09-09: 에셋 조달 중 UI 킷으로 화제 이동. 기록만 하고 복귀
- 2026-09-14: 오프셋 측정 직전, 리듬 코어 컨셉 논의로 화제 이동.
  **단 이번 건은 패턴으로 단정하지 않는다.** 리듬 코어 규칙 미확정은 실제 블로커이고,
  기획서(9번)의 핵심 재료이기도 하다. 11절로 분리해 별도 세션에서 닫기로 함

---

## 8. 작업 관리 규칙

- 하루 4시간: 20분 계획 → 90분 구현 → 휴식 → 85분 구현 → 30분 DEVLOG + 커밋
- 하루 3시간 중 코드 2시간 / 에셋·콘텐츠 1시간. **에셋 1시간 상한 엄수**
- 선택에 막히면 타임박스를 건다. 시간이 다 되면 그 시점의 후보로 확정하고 넘어간다
- DEVLOG.md의 "결정과 근거" 항목이 기획서의 원재료다. 숫자를 정했으면
  왜 그 숫자인지 반드시 남긴다
- 새 아이디어는 9절에 던져만 놓고 현재 작업을 끝낸다
- 3·5절이 실제로 바뀐 날에만 이 문서를 갱신하고 프로젝트 지식에 재업로드한다
- 커밋 메시지는 영어 또는 한글로 통일한다. 로마자 표기는 쓰지 않는다
- 맵 개수는 2개가 상한이다. 맵 개수는 포트폴리오에서 평가 신호가 되지 않는다

### 진행 방식 (2026-09-09 조정)

목적은 학습이 아니라 완성이다. 따라서:

| 영역 | 설명 밀도 |
|---|---|
| 배관 코드(이동·충돌·UI) | 코드 + 에디터 조작만. 원리 설명 생략 |
| Unity 에디터 조작 | 현재 밀도 유지. AI가 대신 눌러줄 수 없는 부분 |
| 숫자 판단(오디오 클럭·판정 윈도우·난이도) | 더 파고든다. DEVLOG에 근거 필수 |

근거: 기획 직군 포트폴리오는 "돌아간다"가 아니라 "왜 이 숫자인가"로 평가된다.
이동 코드를 AI가 썼다고 말해도 감점이 아니지만, 판정 윈도우 근거를 못 대면
그 프로젝트는 본인 것이 아니게 된다.

---

## 9. 보류 중인 아이디어

현재 작업을 끝내기 위해 여기에만 적어 둔 것들. 지정된 시점에만 연다.

### 한글 픽셀 폰트 (블록 J 완료 후, 에셋 시간에 처리)
- 한글 지원 픽셀 폰트 확보 → TextMeshPro Font Asset Creator로 폰트 애셋 생성
- 한글은 글리프가 많아 Atlas Resolution과 Character Set 설정 주의 필요
- **현재 모든 한글 UI가 네모로 출력되는 상태.** 데모 제출 전 필수

### UI 에셋 후보 (여유 있을 때)
- Pixel HUD UI Free Kit (Unity Asset Store)
  https://assetstore.unity.com/packages/2d/gui/pixel-hud-ui-free-kit-378572

### 카메라 경계 제한 (맵 크기 확정 후)
- Cinemachine Confiner 2D. 카메라가 맵 밖(배경색)을 비추지 않도록 제한

### Rule Tile 오토타일 (데모 완성 후 시간 여유 있을 때만)
- 2D Tilemap Extras 패키지 + Rule Tile. 47개 규칙 등록 필요
- 팩에 오토타일 레이아웃 참고 이미지가 Art 폴더에 포함돼 있음

### NPC 말풍선 대사 (데모 완성 후 검토)
- 필수 상호작용 NPC(아이템 수수 등)는 하단 대사창,
  배경 역할 일반 NPC는 머리 위 말풍선으로 분리
- 근거: 짧은 대사에 화면을 가리고 이동을 잠그는 것은 비용 과다
- 구현 비용: World Space Canvas 별도 구성 필요

### 대사 선택지 분기 (데모 완성 후 검토)
- 보스전 진입 확인(예/아니오) 등에 필요
- 견적 약 1일

### Claude Code 도입 (여유 있을 때)
- 로컬 파일을 직접 읽고 쓰므로 PROJECT_STATE.md, DEVLOG.md, C# 스크립트를
  복사-붙여넣기 없이 갱신할 수 있다

---

## 10. 남은 일정 (2026-09-14 → 09-30)

| 블록 | 내용 | 견적 |
|---|---|---|
| **11절** | 리듬 코어 규칙 확정 (별도 세션) | 1~2시간 |
| J | 리듬 보스전 구현 (Conductor → 판정 → 로그) | 4~6일 |
| F-후반 | 지형 경계·소품·캐릭터 스프라이트 적용 | 1~2일 |
| K | 맵 2개 확장 + 맵 간 이동 | 1~2일 |
| 폰트 | 한글 픽셀 폰트 + TMP 애셋 | 반일 |
| 기획서 | 완료 정의 9번 | 1~2일 |

합계 8~13일. 마감까지 16일. **여유가 거의 없다.**
9절 항목을 여는 순간 마감을 넘긴다.

---

## 11. 미확정: 리듬 코어 규칙

**이 항목이 닫히기 전까지 블록 J의 판정 로직은 시작하지 않는다.**

### 문제

PROJECT_STATE 기존 기술("7박째에 스페이스")은 리듬닥터 1스테이지를
그대로 복제하는 것이다. 포트폴리오에서 "레퍼런스를 그대로 가져왔다"로 읽힌다.

### 새 컨셉 방향 (2026-09-14 제기)

음악 장르별 월드(클래식 / 재즈 / 팝 / 락 / 힙합)를 지나가며,
각 월드의 보스전이 **그 장르의 음악적 특징을 규칙으로 변환**한 형태를 갖는다.

### 확정해야 할 것

1. 원버튼·시선 자유를 유지하면서 리듬닥터와 구별되는 코어 규칙
2. 장르별 특징 → 규칙 변환 매핑 (최소 5개 월드 분량의 설계)
3. 기존 난이도 축(정박 → 변박 → 무음 박 → 폴리리듬)과의 관계.
   장르 축과 난이도 축이 충돌하는가, 직교하는가
4. 세계관(2절)을 음악 장르 기반으로 전환할 것인가.
   구매한 타일셋(중세 유럽 판타지)과의 정합성

### 제약 (이건 협상 대상이 아니다)

- **1차 목표에서 구현하는 보스는 1개, 60초, 난이도 1단계뿐이다.**
  5개 월드 설계는 기획서용이지 구현 대상이 아니다
- 1번 보스는 가장 단순한 규칙(정박 기반)을 쓴다.
  따라서 2·3·4번이 미확정이어도 1번 보스 구현은 착수 가능하다
- 이 논의는 **별도 세션에서 1~2시간 타임박스**로 진행하고,
  결론을 이 문서 2절·3-4절에 반영한 뒤 구현으로 복귀한다